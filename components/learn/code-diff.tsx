// Stub — replaced with the real two-pane diff in Plan 4A Task 5.
export function CodeDiff({ children }: { before?: string; after?: string; children?: React.ReactNode }) {
  return <div>{children}</div>
}
