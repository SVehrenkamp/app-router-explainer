// Stub — replaced with the real embed panel in Plan 4A Task 5.
export function ModuleEmbed({ children }: { href?: string; title?: string; children?: React.ReactNode }) {
  return <div>{children}</div>
}
